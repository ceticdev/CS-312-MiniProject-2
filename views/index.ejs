<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Crypto Price Check</title>
  <link href="https://fonts.googleapis.com/css?family=Merriweather" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="container mt-5">
    <h1 class="text-center mb-4">Check Cryptocurrency Price</h1>
    
    <form action="/get-price" method="POST" class="mb-4">
      <div class="mb-3">
        <label for="crypto" class="form-label">Cryptocurrency Symbol:</label>
        <input type="text" id="crypto" name="crypto" class="form-control" placeholder="e.g. BTC" required>
      </div>

      <div class="mb-3">
        <label for="currency" class="form-label">Currency:</label>
        <input type="text" id="currency" name="currency" class="form-control" placeholder="e.g. USD" required>
      </div>

      <button type="submit" class="btn btn-primary">Get Price</button>
    </form>

    <!-- Display price if available -->
    <% if (price) { %>
      <h2 class="text-center text-success">The price of <%= crypto %> in <%= currency %> is: <%= price %></h2>
    <% } %>
    
    <!-- Display error message -->
    <% if (error) { %>
      <p class="text-danger text-center"><%= error %></p>
    <% } %>
  </div>
</body>
</html>
